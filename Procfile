# Optional Procfile examples (not required by Vercel). Keep for reference.
# Web: gunicorn BonCafe.wsgi:application --bind 0.0.0.0:$PORT
# ASGI (for Uvicorn): uvicorn api.asgi:application --host 0.0.0.0 --port $PORT
